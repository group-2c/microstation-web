import Constant from "_constant"
const identif = Constant.appGlobalVar

// Auth
export const AUTH_TOKEN = `${identif}_AUTH_TOKEN`
export const AUTH_USER = `${identif}_AUTH_USER`
export const AUTH_OWNS = `${identif}_AUTH_OWNS`
export const SET_AUTHINFO = `${identif}_SET_AUTHINFO`

// Menu
export const MENU_OPEN_KYES = `${identif}_MENU_OPEN_KYES`
export const MENU_SELECTED_KEYS = `${identif}_MENU_SELECTED_KEYS`

// 配电
export const SET_CIRCUIT_KEY = `${identif}_SET_CIRCUIT_KEY`
export const SET_CIRCUIT_STATUS = `${identif}_SET_CIRCUIT_STATUS`
